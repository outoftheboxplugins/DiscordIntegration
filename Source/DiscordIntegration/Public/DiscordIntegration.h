#pragma once

#include <Modules/ModuleManager.h>

/**
 * Module responsible for compiling, loading and exposing the Discord C++ SDK into Unreal Engine
 */
class FDiscordIntegrationModule : public IModuleInterface
{
};
